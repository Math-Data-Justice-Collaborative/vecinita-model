# Modal model service — local CLI / container overrides.
# Canonical environment catalog: repo root .env.example

MODAL_TOKEN_ID=
MODAL_TOKEN_SECRET=
OLLAMA_HOST=http://localhost:11434
MODELS_PATH=/models
DEFAULT_MODEL=gemma3
